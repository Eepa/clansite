<h1>Join a clan</h1>

<!--  form_for @user, url: user_update_clan_id_path, method: :put do |f| %> -->
<%= form_for @user, url: update_clan_id_user_path, method: :put do |f| %>

    <% if @user.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

          <ul>
            <% @user.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>



        <div class="field">
          <%= f.label :clan_id %><br>
          <%= f.select :clan_id, options_from_collection_for_select(@clans, :id, :name, selected: @user.clan_id) %>
        </div>


    <div class="actions">
      <%= f.submit "Join clan" %>
    </div>
<% end %>
